<article>
  <header>
    <h2>
      <%= @category.name %>
      [ <%= link_to 'Edit', edit_category_path(@category) %> ]
    </h2>
  </header>
    <p>Recipes:</p>
  
       <% @recipes.each do |recipe| %>         
           <div>
                <%= link_to recipe.title,recipe,class:"btn btn-primary btn-block" %>
           </div>
       <%end%>
  
</article>

<p><%= link_to 'Back', categories_path %></p>